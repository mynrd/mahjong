{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Mahjong": {
    "//Rules": "Overrides appsettings.json key by key. Only list what differs for local play; everything left out keeps the value from appsettings.json.",
    "Rules": {
      "//BotPatienceSeconds": "Nothing else rushes a discard now, so this is the only clock worth loosening for local play: a bot in the next seat leaves the tile alone for five minutes instead of twenty seconds while you poke at the table.",
      "BotPatienceSeconds": 300
    }
  }
}
