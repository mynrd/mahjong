{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Mahjong": {
    "//Rules": "Overrides appsettings.json key by key. Only list what differs for local play; everything left out keeps the value from appsettings.json.",
    "Rules": {
      "ClaimWindowSeconds": 300,
      "NoHelperClaimWindowSeconds": 10
    }
  }
}
